## What this changes
A short description of the change and why.

## Checklist
- [ ] `python -m pytest -q` passes locally
- [ ] I ran `python governance/sign_ruleset.py --generate-key` if governance/tests need it
- [ ] No private keys, personal data, or `webui/backend/data/` committed
- [ ] Docs updated if behavior changed
- [ ] This change does **not** weaken an immutable rule (R1, R2, R5, R6, R10) or the local kill-switch

## Related issues
Closes #
