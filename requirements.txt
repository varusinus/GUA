# GUA — Phase 0/1 reference tooling dependencies
cryptography>=42.0      # ed25519 ruleset signing/verification
PyYAML>=6.0             # ruleset parsing
pytest>=8.0             # tests
# Backend bridge (optional, for webui/backend):
flask>=3.0              # local chat API
requests>=2.31          # proxy to a model endpoint (Ollama / OpenAI-compatible)
ddgs>=6.0                 # web search tool (DuckDuckGo, no API key)
numpy>=1.24             # federated training + signed model registry math
